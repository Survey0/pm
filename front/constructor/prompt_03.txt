Now let's add API details.

Here's an example how to call external APIs:

        function fetchData() {
            const resultBox = document.getElementById('result-box');
            resultBox.innerText = "Loading...";
            resultBox.className = "";

            // Call the API endpoint
            fetch('api/get_data.php?id=1')
                .then(response => {
                    if (!response.ok) {
                        throw new Error('HTTP error ' + response.status);
                    }
                    return response.json();
                })
                .then(data => {
                    if (data.error) {
                        resultBox.innerText = "Error: " + data.error;
                        resultBox.className = "error";
                    } else if (data.message) {
                        resultBox.innerText = data.message;
                        resultBox.className = "success";
                    } else {
                        resultBox.innerText = "No data received";
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    resultBox.innerText = "Failed to connect to backend.";
                    resultBox.className = "error";
                });
        }

Important stuff:
(1) APIs are located in api subfolder (relative to the current HTML)
(2) API implementations are not important now
(3) there are the following APIs to integrate with:
- get list of available surveys from database
- save currently edited and saved survey to the database
- delete survey from database (on button click; then reload from DB and re-render the page)

Please make the integrations currently mocked but in understandable way, so that a person without JS/HTML experience could uncomment/replace stub/local storage data with data from actual calls (maybe just a commented-out block of real call right after the mock).