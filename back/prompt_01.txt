I uploaded the current version of the interface: 
constructor.html
index.html
constructor.js
entry.js
styles.css
And the current version of the database schema:
schema.sql
And the JSONs for the APIs: jsons.txt

Analyze the files and show the list of discrepancies/mismatched fields, i.e. anything that would cause an error in runtime. Don't fix errors, just present them to me and then we'll decide what to do.

Acceptable differences:
(1) DB field names are snake_case whereas PHP/JS fields names are camelCase
(2) missing php files